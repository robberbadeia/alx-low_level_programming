#include "main.h"
/**
 * add - Add two integers
 * @i: int type number
 * @j: int type number
 * @z: int type number containe the result of add @i and @j
 * Return: result of addition
 */
int add(int i, int j)
{
	// @z int type number
	int z;

	z = i + j;
	if (z > 9)
	{
		return (z);
		return (z);
	}
	else
	{
		return (z);
	}
}
