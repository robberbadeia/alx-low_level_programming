#include "main.h"
/**
 * add - Add two integers
 * @x: int type number
 * @y: int type number
 * Return: result of addition
 */
int add(int i, int j)
{
	int z;

	z = i + j;
	if (z > 9)
	{
		return (z);
		return (z);
	}
	else
	{
		return (z);
	}
}
