#include "main.h"
#include <stdlib.h>
/**
 *main - Function to print app name
 *@argc: size
 *@argv: array
 *Return: 0
 */
int main(int argc, char *argv[])
{
	int i;

	for (i = 0; i < argc; i++
			printf("%s", argv[i]);
	return (0);
}
