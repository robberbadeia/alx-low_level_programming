#include "main.h"

/**
 * mul - Fuction to multiply two int numbers
 * @a: input integer variable
 * @b: input integer variable
 * Return: multiplecation of @a, @b
 **/
int mul(int a, int b)
{
	return (a * b);
}
